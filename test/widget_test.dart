// Smoke test placeholder.
// Full widget/integration tests require a real device due to SQLite and
// MethodChannel dependencies. Unit tests live in the other test/ files.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () => expect(true, isTrue));
}
